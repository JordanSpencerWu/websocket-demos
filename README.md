Web client application is a web application in React.

## Getting Started

1. You would need to download node.js https://nodejs.org/en/
2. Install project dependencies `npm install`
3. Start development `npm start`
